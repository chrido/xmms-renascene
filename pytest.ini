[pytest]
addopts = -ra -v
console_output_style = progress
testpaths = e2e
markers =
    android: test drives the managed Android emulator through adb
    no_xdotool: test needs a display but does not need xdotool
